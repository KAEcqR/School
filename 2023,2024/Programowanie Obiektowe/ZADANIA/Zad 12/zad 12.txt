#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int X;
    
    cout << "Podaj X: ";
    cin >> X;
    
    if (X > 0 && x <= 10) {
        cout << "f(X) w tym przypadku f(" << X << ") = 2";
    } else if (X > 10 || x <= -10) {
        cout << "f(X) w tym przypadku f(" << X << ") = 0";
    } else {
        cout << "f(X) w tym przypadku f(" << X << ") = 1";
    }
    
    return 0;
}
