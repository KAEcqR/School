#include <iostream>
using namespace std;

int main() {
    const double PI = 3.14159;
    double r;

    cout << "Podaj promień koła: " << endl;
    cin >> r;
    
    if(r>0){
        double obwod = 2 * PI * r;
        double pole = PI * r * r;
    }else{
        cout<<"Źle podane"<<endl;
    }

    cout << "Pole koła wynosi: " << pole << endl;
    cout << "Obwód koła wynosi: " << obwow << endl;

    return 0;
}