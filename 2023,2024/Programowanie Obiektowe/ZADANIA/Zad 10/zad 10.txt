#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int a, b, c, d;

    cout << "Podaj A: ";
    cin >> a;

    cout << "Podaj B: ";
    cin >> b;

    cout << "Podaj C: ";
    cin >> c;

    cout << "Podaj D: ";
    cin >> d;

    if (a >= 0 && b >= 0 && c >= 0 && d >= 0) {
        double s = sqrt(a * b * c * d);
        cout << "Wynik: " << s << endl;
    } else {
        cout << "Wartośni maja byc nie ujemne" << endl;
    }

    return 0;
}