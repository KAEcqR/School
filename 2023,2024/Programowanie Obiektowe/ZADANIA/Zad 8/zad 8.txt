#include <iostream>
#include <stdexcept> // Dodajemy nagłówek dla obsługi wyjątków
using namespace std;

int main() {
    double A;
    double B;

    cout << "Podaj A: " << endl;
    cin >> A;

    cout << "Podaj B: " << endl;
    cin >> B;

    try {
        if (A < 0 && B < 0) {
            cout<<"Wartość musi być dodatnia, daj jeszcze raz"<<endl;
	    throw(A);
	    throw(B)
        }else{
            double Pole = A * B;
        double Obwod = 2 * (A + B);
        cout << "Obwód: " << Obwod << endl;
        cout << "Pole: " << Pole << endl;
        }

        
    } catch (double A) {
        cout << Źle"<< endl;
    }

    return 0;
}